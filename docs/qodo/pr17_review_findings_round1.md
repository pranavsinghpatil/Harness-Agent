# Qodo PR #17 Review Findings

## Qodo Fix Summary — Round 1

Resolved the review findings for scheduler-controlled System 1 execution provenance.

1. Reduced `SandboxEnvironment.step()` by extracting fault, task scheduling, compute completion, deadline, controller, and actuator helpers.
2. Added explicit annotations for scheduler and provenance test locals.
3. Kept delivered sensor packets pending until the perception task completes.
4. Added task-specific `DEADLINE_MISSED` events sourced from scheduler deadline records; late controllers issue no command.
5. Added `TASK_REJECTED` so queue admission failure is distinct from a deadline miss.
6. Added per-step actuator application tracking so `ACTUATOR_APPLIED` is emitted only for newly applied commands.
7. Added canonical event literals for compute, actuator, perception, observation, and rejection lifecycle states.
8. Added immutable `ObservationState` with timestamps, sensor lineage, packet lineage, and decision age.
9. Updated the System 1 provenance contract and frontend event type reference.
10. Added focused tests covering scheduler timestamps and perception availability ordering.

Focused tests were added in `tests/test_system1_execution_provenance.py`. They
could not be executed in this environment because neither `python` nor the
Windows `py` launcher can start; `git diff --check` passed.

Generated by Qodo PR Resolver skill
