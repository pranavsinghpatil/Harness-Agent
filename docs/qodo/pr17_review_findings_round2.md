# Qodo PR #17 Review Findings

## Qodo Fix Summary — Round 2

Resolved the follow-up Qodo findings after the scheduler provenance hardening.

1. Added explicit `ComputeTask` annotations for perception and controller tasks.
2. Added explicit deadline-event mapping annotations in both scheduler paths.
3. Corrected the delayed-perception regression test so completion is delayed without making every observation late and discarded.
4. Changed actuator application provenance to report effective fault-transformed commands, including emergency-stop state.
5. Linked controller decisions to `observation_id` and `observation_age_s` in the canonical command event.
6. Updated the System 1 provenance contract and focused assertions.

Validation: `git diff --check` passed. Python tests remain unavailable because
the environment cannot start `python` or the Windows `py` launcher.

Generated by Qodo PR Resolver skill
